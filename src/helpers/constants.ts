export enum LocationID {
    PCB = 'PCB',
    CYM = 'CYM'
}

export enum TimeFormat {
    '24h' = '24h',
    '12h' = '12h'
}

export enum Color {
    Red = 'red',
    Yellow = 'yellow',
    Green = 'green'
}